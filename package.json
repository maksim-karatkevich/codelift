{
  "name": "codelift",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "cd packages/codelift && yarn build",
    "cra": "cd examples/cra && codelift",
    "next": "cd examples/next && codelift dev",
    "prerelease": "yarn workspace codelift build",
    "release": "np --contents packages/codelift",
    "test": "echo No Tests"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^5.0.0",
    "np": "^5.1.3"
  }
}
