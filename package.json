{
  "private": true,
  "scripts": {
    "dev": "concurrently yarn:dev-cra yarn:dev-gui",
    "dev-cra": "cd examples/cra && yarn start",
    "dev-gui": "cd packages/gui && yarn dev -p 4000"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
