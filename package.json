{
  "private": true,
  "scripts": {
    "cra": "cd examples/cra && codevisor",
    "next": "cd examples/next && codevisor dev"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
